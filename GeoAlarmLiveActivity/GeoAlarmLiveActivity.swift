//
//  GeoAlarmLiveActivity.swift
//  GeoAlarmLiveActivity
//
//  Intentionally emptied 2026-07-11. Xcode's "Add Target" wizard
//  auto-generates a plain home-screen widget stub alongside the Live
//  Activity template — this extension only needs the Live Activity itself
//  (see GeoAlarmLiveActivityWidget.swift), so this stub is unused.
//
//  Left in place (rather than deleted) because this sandbox cannot unlink
//  files outside its own working tree reliably; emptying the content is
//  equivalent for build purposes since PBXFileSystemSynchronizedRootGroup
//  auto-includes every file in this folder regardless of content.
//
